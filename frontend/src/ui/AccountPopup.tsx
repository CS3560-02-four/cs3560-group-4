export default function AccountPopup({ firstName, lastName }: { firstName: string, lastName: string }) {
    return (
        <div>
            {firstName}
            {lastName}
        </div>
    );
}